@model IEnumerable<DavidsCatfishHouse.Models.ShiftChangeRequest>

@{
    ViewBag.Title = "Index";
}

<h2>Shift Change Request</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentEmployee)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NewEmployee)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentEmployee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewEmployee)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>

